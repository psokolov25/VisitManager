# Repository Guidelines

## Структура проекта и модули
- `src/main/java` — исходники Java (`ru.aritmos.*`), точка входа `ru.aritmos.Application`.
- `src/main/resources` — конфигурации и ресурсы (`application.yml`, `logback.xml`, `keycloak.json`).
- Keycloak используется исключительно для интеграции с внешними системами и получения данных пользователей; авторизация реализуется в других подсистемах.
- `src/test/java` — модульные/интеграционные тесты; `src/test/resources` — фикстуры.
- `docs/` — документация/диаграммы; `pom.xml` — настройка Maven; контейнеризация: `Dockerfile`, `docker-compose*.yml`.
- Результат сборки: `target/visitmanager.jar`.

## Сборка, тесты и запуск
- Сборка с тестами: `mvn -s .mvn/settings.xml clean verify`.
- Сборка без тестов: `mvn -s .mvn/settings.xml -DskipTests package`.
- Запуск JAR: `java -jar target/visitmanager.jar`.
- Запуск в dev (если подключен плагин): `mvn -s .mvn/settings.xml mn:run`.
- Тесты: `mvn -s .mvn/settings.xml test`.

- Сетевое окружение использует обязательный HTTP(S)-прокси `proxy:8080`; параметры уже прописаны в `.mvn/jvm.config` и `.mvn/settings.xml`. При работе вне текущей инфраструктуры обновляйте `proxy` и `port` в этих файлах или задавайте собственные значения через `JAVA_TOOL_OPTIONS`/`MAVEN_OPTS` перед запуском Maven. Значение `http.nonProxyHosts` указывайте только через `settings.xml` или переменные окружения (`MAVEN_OPTS`, `JAVA_TOOL_OPTIONS`), чтобы избежать ошибок интерпретации символа `|` в `mvnw.cmd` на Windows (сообщение вида «"127.0.0.1" не является внутренней или внешней командой...»).
- Локально в Docker: `docker compose -f docker-compose.local.yml up -d --build`; остановка — `docker compose -f docker-compose.local.yml down`.

## Стиль кодирования и соглашения
- Java 17; отступ 4 пробела; ~120 символов в строке; UTF‑8.
- Пакеты — нижний регистр (`ru.aritmos.*`); классы — PascalCase; методы/поля — camelCase; константы — UPPER_SNAKE_CASE.
- Используйте Lombok для шаблонного кода (`@Getter`, `@Setter`, `@Builder`).
- Аннотации Micronaut/JSR (`@Singleton`, `@NonNull`, `@Nullable`).
- Логирование через SLF4J/Logback; не используйте `System.out`.
- Не добавляйте бинарные артефакты (изображения JPG/PNG, архивы и т.п.); диаграммы храните в текстовом формате (PlantUML, ASCII).

## Тестирование
- Фреймворки: JUnit 5 + Micronaut Test + Mockito.
- Именование: `*Test` (например, `VisitServiceTest`), структура пакетов как в `main`.
- Юнит‑тесты быстрые; Redis/Kafka — мокать где возможно.
- Запуск: `mvn -s .mvn/settings.xml test`. Порог покрытия не закреплён; целимся в ≥80% для изменённого кода.
- Все реализуемые тесты должны содержать подробные логи всех ключевых действий и проверок; при обнаружении тестов без логов необходимо добавлять соответствующее логирование.

## Коммиты и Pull Request’ы
- Предпочтительно Conventional Commits: `feat(scope): …`, `fix: …`, `refactor: …`, `docs: …`, `chore: …`. Сообщения коммитов должны быть максимально подробными, формулироваться исключительно на русском языке, подробно и детализированно описывать изменения, придерживаться настоящего времени.
- В PR указывайте: что и зачем, связанный issue, план проверки (curl/сценарий), изменения конфигурации/переменных, потенциальные риски.
- CI собирает Docker‑образы из `Dockerfile`; не вносите ломающих изменений без явной мотивации и миграций.
- Слияние веток выполняйте с помощью `git rebase`. Если rebase невозможен (конфликты или иные ограничения), допускается `git merge` с обязательной фиксацией причины в описании PR.
- Перед созданием PR обновляйте основную ветку и рабочую ветку командой `git fetch --all --prune` с последующим `git rebase origin/dev-docs`, чтобы исключить устаревшие коммиты и подготовить чистую историю.
- Все pull request’ы формируйте в ветку `dev-docs`; перед публикацией проверяйте, что рабочая ветка синхронизирована с `origin/dev-docs`, чтобы избежать конфликтов при ревью.
- Разрешайте конфликты локально до публикации PR: проверяйте `git status`, выполняйте `git rebase --continue` и повторно запускайте тесты после решения конфликтов.
- Не переписывайте историю ветки после публикации PR (`git push --force`) без согласования с ревьюерами; при необходимости указывайте причину в описании обновления.
- Перед пушем убеждайтесь, что рабочее дерево чистое (`git status` не содержит «modified»/«untracked»), а локальные коммиты последовательно описывают внесённые изменения.

## Коммуникация и протоколирование
- При выполнении задач каждый шаг сопровождайте явным сообщением или уведомлением, фиксируйте действия в протоколе выполнения.
- Используйте русский язык для всех сообщений, уведомлений и записей протокола.
- Описывайте последовательность шагов, чтобы обеспечивать прозрачность и воспроизводимость процесса работы.

## Лицензия проекта
- Программный продукт распространяется по простой (неисключительной) лицензии.
  Оплата лицензионного вознаграждения осуществляется по модели «Named User».
  Роялти рассчитывается исходя из количества активных пользователей системы в отчётном периоде.
  Расчёт следует методическим рекомендациям Минцифры России по выбору моделей лицензирования отечественного ПО.

## Обновление README
### Общие требования
- Документ оформляется полностью на русском языке в деловом стиле, без жаргона.
- В первом абзаце без заголовка кратко опишите назначение сервиса (Micronaut 4.7.6, Java 17, управление визитами и очередями) одним-двумя предложениями.
- Подчёркивайте, что Keycloak используется для получения данных пользователей и филиалов, а авторизация реализована в Micronaut Security через кастомные правила.
- Ссылки на исходный код и вспомогательные файлы приводите в формате `【F:путь†Lначало-Lконец】` непосредственно после релевантного текста.

### Шапка и бейджи
- Сразу под вводным абзацем размещайте блок бейджей Shield.io строго в порядке: Java → Micronaut → Build → Tests (с якорем на раздел тестов) → Docs → Coverage → Docker → License → Contributing.
- Значения бейджей синхронизируйте с актуальными версиями зависимостей, количеством успешно проходящих тестов и последним отчётом JaCoCo.

### Содержание
- Раздел `## 📑 Содержание` должен точно отражать структуру документа.
- Для вложенных подразделов применяйте многоуровневые списки; порядок пунктов в содержании обязан совпадать с фактическим порядком появления заголовков.

### Заголовки и разметка
- Заголовки уровней `##` и `###` начинайте с тематически подходящей emoji и текста, идентичного пунктам содержания.
- Для пошаговых инструкций используйте нумерованные списки; для перечислений сущностей или файлов — маркированные. Вложенные шаги выделяйте двоичным отступом.
- Команды, HTTP-запросы и фрагменты кода оформляйте в блоках с указанием языка (```bash```, ```java```, ```http```, ```xml```). Короткие идентификаторы и параметры выделяйте инлайновым кодом.
- Описывая REST API, сопровождайте операции примерами запросов и ответов с кодами состояний.

### Обязательные разделы
1. `## 🧾 Обзор` — кратко фиксирует возможности сервиса, перечисляет ключевые интеграции и роль Keycloak.
2. `## ⚙️ Настройка окружения` — включает требования, команды сборки и запуска, описание профилей (включая `local-no-docker`), работу за прокси и правила обращения с временными файлами.
3. `## 🏗️ Архитектура` — описывает слоистую архитектуру и внешние компоненты.
4. `## 🔄 Логика работы` — пошагово раскрывает базовые бизнес-сценарии.
5. `## 🗂️ Структура проекта` — содержит дерево каталогов в блоке кода с короткими комментариями.
6. `## 👥 Инструкции по ролям` — разбивает обязанности по ролям (DevOps, архитектор, аналитик, тестировщик, Front End, Back End, интегратор) с конкретными шагами.
7. `## 📡 REST API` — документирует варианты ответов, контроллеры и дополнительные примеры.
8. Разделы по терминалу клиента, пульту оператора и ресепшену (`## 🕹️`, `## 🖥️`, `## 🖥️ Работа приемной`) — содержат сценарии с последовательными шагами и ссылками на API.
9. Заключительные разделы (`## 📦 Примеры кода`, `## 📊 Диаграммы`, `## 🧑‍💼 Сценарии работы сотрудника`, `## 🤖 Документация для автотестеров`, `## 🆘 Траблшутинг`, `## 🌐 Переменные окружения`, `## 🔗 Полезные ссылки`, `## 🤝 Contributing`, `## 📄 Лицензия`) поддерживайте в актуальном состоянии по мере появления новых возможностей.

### Диаграммы и мультимедиа
- Диаграммы внедряйте как SVG из `docs/diagrams/*.svg` с подписью и ссылкой на PlantUML-исходник (`docs/diagrams/*.puml`).
- Перед изображением добавляйте поясняющий текст, чтобы контекст был понятен без просмотра рисунка.

### Раздел «🧪 Тестирование»
- Начинайте раздел со сводки: команда запуска (`mvn -s .mvn/settings.xml test` или `clean verify`), состояние пайплайна и актуальное покрытие.
- Разбивайте тесты на `### 🧬 Модульные тесты` и `### 🔗 Интеграционные тесты`; внутри группируйте через заголовки `####` по подсистемам и перечисляйте тестовые классы списками.
- При добавлении тестов обновляйте бейдж Tests, показатели покрытия и списки внутри раздела.

### Согласование с исходным кодом
- После модификации тестов запускайте `mvn -s .mvn/settings.xml test`, фиксируйте результаты и отражайте их в разделе «История покрытия тестами» этого файла.
- В сценариях для ролей, терминалов, пультов и автотестеров приводите ссылки на соответствующие контроллеры, сервисы и примеры запросов.

### Последовательность действий и сценарии
- Пользовательские сценарии описывайте структурированными нумерованными списками, каждый шаг сопровождайте пояснением и при необходимости командами/HTTP-запросами.
- В кейсах указывайте используемые эндпоинты, тела запросов, коды ответов и ожидаемые результаты.



## Безопасность и конфигурация
- Не коммитьте секреты. Для разработки — `.env.local`; для стендов — переменные CI/CD.
- Ключевые переменные: `OIDC_ISSUER_URL`, `OAUTH_CLIENT_ID/SECRET`, `REDIS_SERVER`, `KAFKA_SERVER`, `LOKI_SERVER`.
- Профиль Micronaut по умолчанию — `dev` (см. `Application.Configurer`); переопределение — `MICRONAUT_ENVIRONMENTS`.

## История покрытия тестами и требования к новым сценариям
- 2025-09-17: выполнена команда `mvn -s .mvn/settings.xml clean verify`; успешно пройдено 360 автотестов, сформирован отчёт JaCoCo.
- 2025-09-17: повторно выполнена команда `mvn -s .mvn/settings.xml clean verify`; успешно пройдено 368 автотестов. Совокупное покрытие JaCoCo: 43.68 % инструкций, 47.00 % строк, 29.92 % ветвей, 39.42 % сложности и 73.26 % методов (подробности — `target/site/jacoco/jacoco.csv`).
- 2025-09-17: выполнена команда `mvn -s .mvn/settings.xml test`; успешно пройдено 380 автотестов. Совокупное покрытие JaCoCo: 43.89 % инструкций, 47.07 % строк, 31.13 % ветвей, 40.60 % сложности и 73.26 % методов (подробности — `target/site/jacoco/jacoco.csv`).
- 2025-09-17: выполнена команда `mvn -s .mvn/settings.xml test`; успешно пройдено 412 автотестов. Совокупное покрытие JaCoCo: 51.08 % инструкций, 54.77 % строк, 37.14 % ветвей, 43.65 % сложности и 76.66 % методов (подробности — `target/site/jacoco/jacoco.csv`).
- 2025-09-17: выполнена команда `mvn -s .mvn/settings.xml test`; успешно пройдено 416 автотестов. Совокупное покрытие JaCoCo: 52.57 % инструкций, 56.33 % строк, 38.05 % ветвей, 43.93 % сложности и 76.82 % методов (подробности — `target/site/jacoco/jacoco.csv`).
- 2025-09-17: выполнена команда `mvn -s .mvn/settings.xml test`; успешно пройдено 421 автотест. Совокупное покрытие JaCoCo: 54.03 % инструкций, 57.86 % строк, 39.02 % ветвей, 44.27 % сложности и 76.99 % методов (подробности — `target/site/jacoco/jacoco.csv`).
- 2025-09-17: выполнена команда `mvn -s .mvn/settings.xml test`; успешно пройдено 425 автотестов. Совокупное покрытие JaCoCo: 55.49 % инструкций, 59.35 % строк, 39.93 % ветвей, 44.55 % сложности и 77.15 % методов (подробности — `target/site/jacoco/jacoco.csv`).
- 2025-09-17: выполнена команда `mvn -s .mvn/settings.xml test`; успешно пройдено 428 автотестов. Совокупное покрытие JaCoCo: 56.88 % инструкций, 60.78 % строк, 40.84 % ветвей, 44.97 % сложности и 77.63 % методов (подробности — `target/site/jacoco/jacoco.csv`).
- 2025-09-17: выполнена команда `mvn -s .mvn/settings.xml test`; успешно пройдено 432 автотеста. Совокупное покрытие JaCoCo: 58.25 % инструкций, 62.16 % строк, 41.63 % ветвей, 45.25 % сложности и 78.12 % методов (подробности — `target/site/jacoco/jacoco.csv`).
- 2025-09-17: выполнена команда `mvn -s .mvn/settings.xml test`; успешно пройдено 435 автотестов. Совокупное покрытие JaCoCo: 59.52 % инструкций, 63.49 % строк, 42.66 % ветвей, 45.59 % сложности и 78.44 % методов (подробности — `target/site/jacoco/jacoco.csv`).
- 2025-09-17: выполнена команда `mvn -s .mvn/settings.xml test`; успешно пройдено 441 автотест. Совокупное покрытие JaCoCo: 60.77 % инструкций, 64.80 % строк, 43.45 % ветвей, 45.66 % сложности и 78.77 % методов (подробности — `target/site/jacoco/jacoco.csv`).
- 2025-09-18: выполнена команда `mvn -s .mvn/settings.xml test`; успешно пройдено 454 автотеста. Совокупное покрытие JaCoCo: 69.92 % инструкций, 74.04 % строк, 48.60 % ветвей, 46.84 % сложности и 80.55 % методов (подробности — `target/site/jacoco/jacoco.csv`).
- 2025-09-18: повторно выполнена команда `mvn -s .mvn/settings.xml test`; успешно пройдено 454 автотеста. Совокупное покрытие JaCoCo: 69.92 % инструкций, 74.04 % строк, 48.54 % ветвей, 46.77 % сложности и 80.55 % методов (подробности — `target/site/jacoco/jacoco.csv`).
- 2025-09-18: выполнена команда `mvn -s .mvn/settings.xml test`; успешно пройдено 454 автотеста. Совокупное покрытие JaCoCo: 69.92 % инструкций, 74.04 % строк, 48.54 % ветвей, 46.77 % сложности и 80.55 % методов (подробности — `target/site/jacoco/jacoco.csv`, отчёт сформирован в 05:40 UTC).
- 2025-09-18: выполнена команда `mvn -s .mvn/settings.xml test`; успешно пройдено 461 автотест. Совокупное покрытие JaCoCo: 73.67 % инструкций, 77.88 % строк, 50.73 % ветвей, 47.68 % сложности и 81.36 % методов (подробности — `target/site/jacoco/jacoco.csv`, отчёт сформирован в 06:08 UTC).
- 2025-09-18: выполнена команда `mvn -s .mvn/settings.xml test`; успешно пройдено 466 автотестов. Совокупное покрытие JaCoCo: 76.71 % инструкций, 81.02 % строк, 52.37 % ветвей, 48.16 % сложности и 82.17 % методов (подробности — `target/site/jacoco/jacoco.csv`, отчёт сформирован в 06:39 UTC).
- 2025-09-18: выполнена команда `mvn -s .mvn/settings.xml test`; успешно пройдено 466 автотестов. Совокупное покрытие JaCoCo: 76.71 % инструкций, 81.02 % строк, 52.37 % ветвей, 48.16 % сложности и 82.17 % методов (подробности — `target/site/jacoco/jacoco.csv`, отчёт сформирован в 06:39 UTC).
- 2025-09-18: выполнена команда `mvn -s .mvn/settings.xml test`; успешно пройдено 471 автотест. Совокупное покрытие JaCoCo: 78.88 % инструкций, 83.42 % строк, 53.64 % ветвей, 48.72 % сложности и 82.98 % методов (подробности — `target/site/jacoco/jacoco.csv`, отчёт сформирован в 07:10 UTC).
- 2025-09-18: выполнена команда `mvn -s .mvn/settings.xml test`; успешно пройдено 476 автотестов. Совокупное покрытие JaCoCo: 79.05 % инструкций, 83.60 % строк, 54.00 % ветвей, 48.99 % сложности и 82.98 % методов (подробности — `target/site/jacoco/jacoco.csv`, отчёт сформирован в 18:20 UTC).

- Приоритетные области без покрытия (требуют добавления тестов с подробным логированием шагов и проверок, снабжённых документацией в коде):
- `ru.aritmos.service.VisitService` — без покрытия остаются 22 метода (676 строк). В приоритете сложные цепочки `visitCallForConfirmWithMaxLifeTime(...)`, `stopServingAndBackToQueue(...)`, перевод визитов в сервисные пулы (`visitTransferFromQueueToServicePointPool(...)`, `visitTransferToServicePointPool(...)`) и сценарии работы с outcome/документооборотом.
- `ru.aritmos.api.ServicePointController` — 55 методов без покрытия (157 строк): переносы между очередями и пулами, работа с delivered services, маркерами, заметками и исходами по‑прежнему выполняются без автопроверок.
- `ru.aritmos.model.Branch` — 16 методов без покрытия (124 строки), включая `updateVisit(...)` и связанные лямбда‑обработчики сегментации, очередей и сервисов.
- `ru.aritmos.keycloack.service.KeyCloackClient` — 17 методов без покрытия (138 строк): получение филиалов и пользователей, валидация ролей и завершение сессий остаются слепыми зонами.
- Дополнительные точки роста: `ru.aritmos.service.BranchService` (45 строк без покрытия, критичен `openServicePoint(...)`), `ru.aritmos.service.rules.MaxLifeTimeCallRule` (один пограничный сценарий), `ru.aritmos.docs.CurlCheatsheetGenerator`, `ru.aritmos.events.services.EventService` (не хватает проверок генерации событий и экспорта шпаргалок).
- При добавлении новых тестов фиксируйте свежие результаты покрытия в данном файле и обязательно актуализируйте `README.md`: обновляйте бейджи, численность тестов и описания сценариев в списках, чтобы документация оставалась в актуальном состоянии.
- Все названия задач, сообщения коммитов, комментарии к тестам и логирование выполняйте на русском языке.
